[
    {
        "name": "$joinVC",
        "description": "This function will make the bot join a voice channel and play music.",
        "all": "$joinVC[channelId?;deaf?]",
        "example": "No example provided.",
        "params": [
            {
                "name": "channelId?",
                "type": "string",
                "required": false,
                "description": "The channel ID to join."
            },
            {
                "name": "deaf?",
                "type": "string",
                "required": false,
                "description": "Whether to deaf the bot or not"
            }
        ]
    },
    {
        "name": "$playTrack",
        "description": "Plays a track from a given url or search query.",
        "all": "$playTrack[query;engine?]",
        "example": "No example provided.",
        "params": [
            {
                "name": "query",
                "type": "string",
                "required": true,
                "description": "The query to search for."
            },
            {
                "name": "engine?",
                "type": "string",
                "required": false,
                "description": "The engine to use to search for."
            }
        ]
    }
]
